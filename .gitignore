# Dependencies
node_modules/

# Secrets
.env

# NOTE: If you want posts & uploaded images to appear on hosts like Vercel
# (where the filesystem isn't persistent), you must deploy your current data.
# So we DO track these by default.
#
# If you prefer keeping content out of git, re-add these ignores and use a real DB.

# Runtime data (JSON DB)
# data/posts.json

# Uploaded files
# public/uploads/

# OS / Editor
.DS_Store
Thumbs.db
.vscode/
